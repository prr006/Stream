# Python
__pycache__/
*.pyc
.venv/

# Local secrets — NEVER commit these
.env
token.json

# Extracted subtitle / probe / remux caches (repo-root cache/ created at runtime)
cache/

# Vendored browser libs (~40-70 MB) — regenerate via backend/fetch_vendor.py
frontend/vendor/
