fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
httpx>=0.27,<1.0
python-dotenv>=1.0
# Bundled static ffmpeg binary — only used if no system ffmpeg is installed.
# (Ships ffmpeg only; ffprobe JSON probing activates when ffprobe is on PATH.)
imageio-ffmpeg>=0.5
